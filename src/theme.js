import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1A237E",
    },
    secondary: {
      main: "#C5CAE9",
    },
    background: {
      default: "#FFFFFF",
    },
    text: {
      primary: "#212121",
    },
  },
});

export default theme;
